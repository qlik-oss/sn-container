import React from 'react';

export default function Container() {
  const containerStyle = {
    height: '100%',
    width: '100%',
  };

  return (
    <div className="container-root" style={containerStyle}>
      Container Root
    </div>
  );
}
